# Pinned to exact versions for reproducible images and reviewable Dependabot
# bumps (matches the SHA-256-pinned vendored frontend assets in the Dockerfile).
flask==3.1.3
prometheus-client==0.25.0
gunicorn==26.0.0
